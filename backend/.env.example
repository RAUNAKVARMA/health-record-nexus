# Local default: SQLite (works without Docker/Postgres install)
DATABASE_URL=sqlite:///./health_record.db

# When Docker Postgres / Render is running, use:
# DATABASE_URL=postgresql+psycopg2://health:health@localhost:5432/health_record

JWT_SECRET=change-me-in-production
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://health-record-nexus-secure.vercel.app

# Supabase Storage (files) — do NOT commit real service_role key
SUPABASE_URL=https://dwqnpfjaxnkbvjrypdgv.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_BUCKET=health-record-files
